using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Coroutine : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    //IEnumerator Jump()
    //{
    //    float t = 0;
    //    float duration = .5f;

    //    Vector3 startPos = transform.position;

    //    while (t < duration)
    //    {

    //        t += Time.deltaTime;

    //        transform.position = Vector3.Lerp(startPos, new Vector3(0, 2, 0), curve.Evaluate(t / duration));

    //        Debug.Log("JUMP");


    //        yield return null;
    //    }

    //    isJumping = false;
    //}
}
